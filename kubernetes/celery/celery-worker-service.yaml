apiVersion: v1
kind: Service
metadata:
  name: celery-worker
spec:
  selector:
    app: celery
    component: worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000  # or the port you want to expose
